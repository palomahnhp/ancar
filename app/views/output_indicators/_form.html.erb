<%= form_for(@entry_indicator) do |f| %>
  <% if @entry_indicator.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@entry_indicator.errors.count, "error") %> prohibited this entry_indicator from being saved:</h2>

      <ul>
      <% @entry_indicator.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
