<div class="row" >
  <div class="small-12 columns">
    <h1 class="text-center">Carga de RPT</h1>

    <div class="row" >
      <p class="note">
        Para cargar la RPT de una organización se debe partir de un csv o excel el siguiente formato y cabecera:
        <span> <%= link_to "RPT.xlsx", "/carga_rpt/RPT.xlsx" %> </span>
        <br>Elije el excel a importar con el botón Choose File:
        <div class="small-12 columns">
          <%= form_tag import_admin_rpts_path(year: @year), multipart: true do %>
            <div class="medium-4 columns">
              <%= file_field_tag(:file) %>
            </div>
            <div class="medium-2 columns end">
              <%= submit_tag "Import", class: "hollow button" %>
            </div>
          <% end %>
        </div>
      </p>
    </div>

    <div class="row" >
      <p  class="note">
        Exportar la RPT de todas las organizaciones mostradas:
        <%= link_to "Excel", admin_rpts_path(format: "xls") %> |
        <%= link_to "CSV", admin_rpts_path(format: "csv") %>
      </p>
    </div>

    <div class="row">
      <div class="small-12 columns">
        <% if @year.present? %>
          <div class="small-6 medium-4 columns">
            <strong> Último año cargado: <%= @year %> </strong>
          </div>
        <% else %>
            <strong class="text-center"> No hay datos de RPT cargados </strong>
        <% end %>

        <table class="unit-status">
          <tr>
            <th> Cargada </th>
            <th> Organizacion </th>
            <th> Ocupados </th>
            <th> Vacantes </th>
          </tr>

          <% @organizations.each do |organization|%>
            <tr>
              <td class="check">
                <span class="<%= organization_loaded?(organization, @year) %>"></span></td>
              <td> <%= organization.description %> </td>
              <td> <%= organization.rpts.by_year(@year).count %> </td>
              <td> <%= organization.rpts.by_year(@year).vacant.count %> </td>
            </tr>
          <% end %>

        </table>
      </div>
    </div>
  </div>
</div>