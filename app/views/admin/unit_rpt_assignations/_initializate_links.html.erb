<div class="small-12 columns">
  <p> Inicializar asignación de unidades </p>
  <ul class="note">
    <li>
      Básica (solo departamentos):
      <%= link_to "Inicializar", init_admin_unit_rpt_assignations_path(year: @process_year, init: true), class: "button hollow small" %>
    </li>
    <li>
      Desde fichero (<span> <%= link_to "AssignUnits.xlsx", "/cargas/AssignUnits.xlsx" %> </span>)
      <%= form_tag import_admin_unit_rpt_assignations_path(year: @process_year), multipart: true do %>
        <div class="small-12 columns">
          <%= file_field_tag(:file) %> <br>
          <%= submit_tag "Importar", class: "hollow button small" %>
        </div>
      <% end %>
    </li>
    <% if @assignation_year !=  @process_year %>
      <li>
        Copia (se muestran los datos de <%= @assignation_year %>  y se copiaran en <%= @process_year %>):
        <%= link_to "Copiar asignaciones", copy_admin_unit_rpt_assignations_path(year: @process_year, copy: true), class: "button hollow small" %>
      </li>
    <% end %>
  </ul>
</div>
