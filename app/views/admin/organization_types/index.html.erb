<p id="notice"><%= notice %></p>

<h1>Listing Admin Organization Types</h1>

<table>
  <thead>
    <tr>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @admin_organization_types.each do |admin_organization_type| %>
      <tr>
        <td><%= link_to 'Show', admin_organization_type %></td>
        <td><%= link_to 'Edit', edit_admin_organization_type_path(admin_organization_type) %></td>
        <td><%= link_to 'Destroy', admin_organization_type, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Organization type', new_admin_organization_type_path %>
