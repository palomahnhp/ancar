<h2><%= t(" supervisor.roles.index.title.#{params[:role_name]}") %></h2>

<div class="row">
  <%= form_tag search_ supervisor_roles_path(role_name: params[:role_name]), method: :get, remote: true  do %>
      <div class="small-12 medium-6 column">
        <%= text_field_tag :search, '', placeholder: t(' supervisor.roles.search.login_placeholder') %>
      </div>
      <div class="small-12 medium-6 column">
        <%= submit_tag t(' supervisor.roles.search.search'), class: 'button' %>
      </div>
  <% end %>
</div>

<div id="search-result" class="row"></div>


<table id="supervisors">
  <% @users.each do |user| %>
      <tr>
        <td>
          <%= link_to user.login, edit_ supervisor_user_path(user) %>
        </td>
        <td>
          <%= link_to user.full_name, edit_ supervisor_user_path(user) %>
        </td>
        <td>
          <%= user.official_position %>
        </td>
        <td>
          <%= user.organization_description %>
        </td>
        <td class="text-right">
          <% if user.has_role? params[:role_name], :any %>
              <%= link_to t(' supervisor.roles.action.add_resource'),
                          {controller: ' supervisor/roles', action: :create, role_name: params[:role_name], user_id: user.id, add_resource: 'new' },
                          method: :post, class: 'button success hollow'
              %>

              <%= link_to t('supervisor.roles.action.delete'),
                          supervisor_role_path(user.roles.where(name: params[:role_name]).first, role_name: params[:role_name], user_id: user.id),
                          method: :delete,
                          class: 'button hollow alert'
              %>

          <% else %>
              <%= link_to t('supervisor.roles.action.add'),
                          {controller: 'supervisor/roles', action: :create, role_name: params[:role_name], user_id: user.id },
                          method: :post, class: 'button success hollow'
              %>
          <% end %>
        </td>
      </tr>
  <% end %>
</table>

<%= paginate @users %>

