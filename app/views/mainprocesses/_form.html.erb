<%= form_for(@mainprocess) do |f| %>
  <% if @mainprocess.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@mainprocess.errors.count, "error") %> prohibited this mainprocess from being saved:</h2>

      <ul>
      <% @mainprocess.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
