<div class="row" >
  <p>
    <%= link_to(t('actions.back'), eval("#{namespace}_first_level_units_path"), class:"icon-angle-left" ) %>
  </p>
</div>

<div class="row">
  <div class="small-12 columns" >
    <h1><%= t('shared.first_level_unit.title')%></h1>
  </div>

  <p class="note">
    Importar las Áreas/Distritos partiendo de un excel con el siguiente formato y cabecera:
    <span> <%= link_to "Areas.xlsx", "/carga_rpt/Areas.xlsx" %> </span>
    <br>Elije el excel a importar con el botón Choose File:

    <div class="small-12 columns">
      <%= form_tag import_admin_first_level_units_path, multipart: true do %>
        <div class="medium-4 columns">
          <%= file_field_tag(:file) %>
        </div>
        <div class="medium-2 columns end">
          <%= submit_tag "Import", class: "hollow button" %>
        </div>
        <div class="medium-4 columns end">
          <%= link_to(t("actions.create"), new_admin_first_level_unit_path, class: "hollow button") %>
        </div>
      <% end %>
    </div>
  </p>

  <div class="small-12 columns" >
    <table class="unit-status">
      <thead>
        <tr>
          <th> <%= t('activerecord.attributes.first_level_unit.den_unit') %> </th>
          <th> <%= t('activerecord.attributes.first_level_unit.organization_id') %> </th>
          <th> <%= t('activerecord.attributes.first_level_unit.period_from') %> </th>
          <th> <%= t('activerecord.attributes.first_level_unit.period_to') %> </th>
          <th> <%= t('activerecord.attributes.first_level_unit.sapid_unit') %> </th>
          <th colspan="2"> <%= t('shared.first_level_unit.index.actions') %> </th>
        </tr>
      </thead>

      <tbody>
        <% @first_level_units.each do |first_level_unit| %>
          <tr>
            <td> <%= first_level_unit.den_unit %> </td>
            <td> <%= first_level_unit.organization&.description %> </td>
            <td> <%= first_level_unit.period_from %> </td>
            <td> <%= first_level_unit.period_to %> </td>
            <td> <%= first_level_unit.sapid_unit %> </td>
            <td> <%= link_to "", edit_admin_first_level_unit_path(first_level_unit), class: "icon-pencil"  %></td>
            <td> <%= link_to "", admin_first_level_unit_path(first_level_unit), method: :delete,
                             data: { confirm: 'Are you sure?' }, class: "icon-trash" %></td>
          </tr>
        <% end %>
      </tbody>
    </table>
    <br>
  </div>
</div>